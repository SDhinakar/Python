# ceil() function is used to return the smallest integer value greater than or equal to the specified expression or value.
# In short ,it is diferent from round() by the fact that it always rounds up the number though it is 1.1 it gives 2.

import math
a=1.1
b=1
c=a+b
print(math.ceil(c))